pub mod bird;
// pub mod cli;
pub mod kernel;

// use async_tempfile::TempFile;
// use tokio::io::AsyncWriteExt;

// pub async fn str_to_file(content: &[u8]) -> Result<TempFile, async_tempfile::Error> {
//   let mut file = TempFile::new().await?;
//   file.write_all(content).await?;
//   file.flush().await?;
//   Ok(file)
// }
